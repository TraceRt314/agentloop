# ──────────────────────────────────────────────────────────────
# AgentLoop — Environment Configuration
# Copy to .env and adjust values as needed:  cp .env.example .env
# ──────────────────────────────────────────────────────────────

# ── Core ──────────────────────────────────────────────────────
AGENTLOOP_DATABASE_URL=sqlite:///./agentloop.db
AGENTLOOP_API_HOST=0.0.0.0  # listens on all interfaces (accessible from LAN)
AGENTLOOP_API_PORT=8080
AGENTLOOP_API_BASE_URL=http://localhost:8080
AGENTLOOP_DEBUG=true
AGENTLOOP_LOG_LEVEL=INFO

# ── LLM Provider ─────────────────────────────────────────────
# Supported providers: ollama, openai, openrouter, or any OpenAI-compatible API.
# Default: Ollama running locally (no API key needed).
#
# To use OpenAI:
#   AGENTLOOP_LLM_PROVIDER=openai
#   AGENTLOOP_LLM_MODEL=gpt-4o-mini
#   AGENTLOOP_LLM_BASE_URL=https://api.openai.com/v1
#   AGENTLOOP_LLM_API_KEY=sk-...
#
# To use OpenRouter:
#   AGENTLOOP_LLM_PROVIDER=openrouter
#   AGENTLOOP_LLM_MODEL=anthropic/claude-sonnet-4-6
#   AGENTLOOP_LLM_BASE_URL=https://openrouter.ai/api/v1
#   AGENTLOOP_LLM_API_KEY=sk-or-...

AGENTLOOP_LLM_PROVIDER=ollama
AGENTLOOP_LLM_MODEL=llama3.2
AGENTLOOP_LLM_BASE_URL=http://localhost:11434/v1
AGENTLOOP_LLM_API_KEY=

# ── Plugins ───────────────────────────────────────────────────
# Comma-separated list of plugins to enable (empty = all).
# For a minimal setup without OpenClaw/Mission Control:
AGENTLOOP_ENABLED_PLUGINS=llm,knowledge,office-viz,tasks

# ── Timeouts ──────────────────────────────────────────────────
AGENTLOOP_STEP_TIMEOUT_SECONDS=300
AGENTLOOP_AGENT_WORK_INTERVAL_SECONDS=300
AGENTLOOP_ORCHESTRATOR_TICK_INTERVAL_SECONDS=300

# ── OpenClaw Gateway (optional, not needed for local use) ────
# AGENTLOOP_OPENCLAW_GATEWAY_URL=ws://localhost:18789
# AGENTLOOP_OPENCLAW_GATEWAY_TOKEN=

# ── Mission Control (optional, not needed for local use) ─────
# AGENTLOOP_MC_BASE_URL=http://localhost:8002
# AGENTLOOP_MC_TOKEN=
# AGENTLOOP_MC_ORG_ID=
# AGENTLOOP_BOARD_MAP={}
